/* Clean, modern style reset */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
}

body {
    background-color: #f4f7f6;
    color: #333;
    line-height: 1.6;
    display: flex;
    flex-direction: column;
    min-height: 100vh;
}

/* Header & Navigation Bar */
header {
    background-color: #0b2240; /* Royal Blue/Navy */
    color: white;
    padding: 1.5rem 10%;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.logo {
    font-size: 1.4rem;
    font-weight: bold;
}

nav a {
    color: #bdc3c7;
    text-decoration: none;
    margin-left: 20px;
    font-weight: 600;
}

nav a:hover, nav a.active {
    color: #f39c12; /* Premium Gold/Orange highlight */
}

/* Main Content Area */
main {
    flex: 1;
    padding: 3rem 10%;
}

.hero {
    text-align: center;
    padding: 4rem 1rem;
    background: white;
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0,0,0,0.05);
}

.hero h1 {
    font-size: 2.5rem;
    color: #0b2240;
    margin-bottom: 1rem;
}

.hero p {
    font-size: 1.2rem;
    margin-bottom: 2rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
}

/* Buttons */
.cta-btn {
    display: inline-block;
    background-color: #f39c12;
    color: white;
    padding: 0.8rem 2rem;
    text-decoration: none;
    font-weight: bold;
    border-radius: 5px;
    border: none;
    cursor: pointer;
    font-size: 1rem;
}

.cta-btn:hover {
    background-color: #e67e22;
}

/* Form Styling */
.quote-form {
    background: white;
    padding: 2rem;
    border-radius: 8px;
    max-width: 500px;
    margin: 2rem auto;
    box-shadow: 0 4px 6px rgba(0,0,0,0.05);
}

.quote-form label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: bold;
}

.quote-form input, .quote-form textarea {
    width: 100%;
    padding: 0.8rem;
    margin-bottom: 1.5rem;
    border: 1px solid #ccc;
    border-radius: 4px;
}

.contact-box {
    text-align: center;
    background: #eaf2f8;
    padding: 1.5rem;
    border-radius: 8px;
    margin-bottom: 2rem;
}

/* Footer */
footer {
    background-color: #0b2240;
    color: white;
    text-align: center;
    padding: 1rem;
    margin-top: auto;
    font-size: 0.9rem;
}
